<!DOCTYPE html>
<html>
<head>
    <link rel="stylesheet" href="../../stylesheets/import/bootstrap.css"> <!-- load bootstrap css -->
    <link rel="stylesheet" href="../../stylesheets/import/fontawesome.css"> <!-- load fontawesome -->
    <link rel="stylesheet" href="../../stylesheets/style.css"> <!-- load own css -->
    <title>
        {{#if title}}
            {{title}} - Pokémon
        {{else}}
            Insert Title Here
        {{/if}}
    </title>
</head>
<body>
    <div class="row header pull-left"><a class="btn btn-info" style="margin-left: 20px;" href="/">Home</a></div>
    <div class="header pull-right">
        {{#if user}}
        <span style="margin-right: 20px;">
            {{#if user.local.email}}
                Logged in as: {{user.local.email}}
            {{else if user.google.name}}
                Logged in as: {{user.google.name}}
            {{else}}
                Logged in as: {{user.facebook.name}}
            {{/if}}
            {{#ifCond user.role '==' 'admin'}}
            [ADMIN]
            {{/ifCond}}            
        </span>
        {{/if}}
    </div>
    <div class="row">
    {{{body}}}
    </div>

</body>
</html>